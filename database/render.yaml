services:
  - type: web
    name: house-analysis-api
    runtime: python
    buildCommand: cd database && pip install -r requirements.txt
    startCommand: cd database && uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11